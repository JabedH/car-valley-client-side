import React from "react";

const Blog = () => {
  return (
    <div>
      <div>
        <h1>Difference between Node.JS and Javascript</h1>
        NodeJS is a Javascript runtime environment that allows the javascript to
        be run on the server-side. Javascript is a Scripting language it also a
        high-level programming language. Javascript can only be run in the
        browsers.Nodejs allows Javascript code to run outside the browser. It is
        basically used on the client-side.It is mostly used on the server-side.
        JS have capability to add HTML tags. Nodejs does not have capability to
        add HTML tags. Javascript can run in any browser engine. Javascript is
        used in frontend development and node.js is used in backend development.
      </div>
    </div>
  );
};

export default Blog;
